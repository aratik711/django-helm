# Default values for django.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: aratik711/django
  tag: 2.1
  pullPolicy: Always

service:
  type: NodePort
  port: 8000
  targetPort: 8000
  nodePort: 30008

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
   requests:
    cpu: 200m
    memory: 256Mi

nodeSelector: {}

tolerations: []

affinity: {}

env:
  # not consumed by pods directly
  #dbInstance: PROJECT_ID:europe-west1:SQL_DB_NAME
  staticFiles: "/static/"
  djangoappLogLevel: "DEBUG"
  DJANGO_APP_DB_ENGINE: "postgresql_psycopg2"
  POSTGRES_DBNAME: "gitranger"
  POSTGRES_USER: "gituser"
  POSTGRES_PASSWORD: "rGswMtQejL7g49e"
  POSTGRES_HOST: "172.17.0.2"
  POSTGRES_PORT: "5432"